import { InputFocoDirective } from './input-foco.directive';

describe('InputFocoDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFocoDirective();
    expect(directive).toBeTruthy();
  });
});
